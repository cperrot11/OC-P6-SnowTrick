<form class="form-hidden" method="post" action="{{ path('trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cette figure ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="fas fa-trash-alt text-right" data-toggle="tooltip" title="Supprimer"></button>
</form>
